package hext;

/**
 * The Nil enum can be used as an alternative to Null for situations
 * where dynamic/anonymous structures are not allowed
 * - like for type parameters or argument types.
 */
enum Nil {}
